language: csharp
mono: none
dotnet: 1.1.5
script:
- dotnet restore
deploy:
  provider: gae
  skip_cleanup: true
  keyfile: credential.json
  project: "${GOOGLE_PROJECT_ID}"
  default: true
  on:
    all_branches: true
